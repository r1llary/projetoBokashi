import { HeaderContainer, Logo} from "./Header.styles";

const Header = () => {
  
    return (
        <HeaderContainer>
          <Logo> Bokashi. </Logo>
        </HeaderContainer>
    );
}
export default Header;